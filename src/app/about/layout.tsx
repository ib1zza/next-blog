import React, {PropsWithChildren} from 'react';

const LayoutAbout: React.FC<PropsWithChildren> = ({children}) => {
    return (
        <div>
            {/*Layout about*/}
            {children}
        </div>
    );
};

export default LayoutAbout;
